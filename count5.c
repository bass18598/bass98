#include<stdio.h>
#include<conio.h>
main()
{
 char s[500];
 int i,count=0;
 printf("\n Enter the string");
 scanf("%d",&s);
 for(i=0;str[i]!='\0';i++)
 {
  if(s[i]>='0'&&s[i]<=9)
  {
   count++;
   }
   printf("%d", count);
   }
   
